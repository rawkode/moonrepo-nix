# Moon toolchain configuration for devenv example
nix:
  plugin: "file://../../moon_toolchain_nix.wasm"
  useFlake: false
  useShellNix: false
  useFlox: false
  useDevenv: true

# Example configurations for tools managed by devenv
node:
  version: "20.0.0"
  packageManager: "yarn"

go:
  version: "1.21.0"