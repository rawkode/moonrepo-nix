# Project configuration for Flox example
type: "application"
language: "javascript"

toolchain:
  nix:
    disabled: false

tasks:
  hello:
    command: "echo"
    args:
      - "Hello from Flox environment!"
    
  deno-version:
    command: "deno"
    args:
      - "--version"
  
